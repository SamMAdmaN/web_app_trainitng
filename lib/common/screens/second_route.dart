import 'package:flutter/material.dart';

class SecondRoute extends StatelessWidget {
  const SecondRoute() : super();

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
